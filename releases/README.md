# Release artifacts folder

This folder is used by `electron-builder` (`npm run build-win`) to place packaged app artifacts.

- Default output: `releases/`
- Do not commit generated executables from this folder